# Project screenshots

Drop the two screenshots you attached in chat here, with these exact filenames.
The cards reference them by these paths and hide the image if the file is missing.

| Save as                     | Source screenshot                                  | Shown on card       |
| --------------------------- | -------------------------------------------------- | ------------------- |
| `drobeo.png`                | Drobeo mobile home screen (AI Stylist / wardrobe)  | Drobeo              |
| `testflow-recorder.png`     | TestFlow Recorder dashboard (Chrome test recorder) | Developer Tooling   |

Recommended: crop to roughly a 16:10 landscape frame and keep width around
1200–1600px. PNG or JPG both work — if you use `.jpg`, update the `image:`
paths in `src/data.js` to match.
